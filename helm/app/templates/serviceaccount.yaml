{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "demo-app.serviceAccountName" . }}
  labels: { app.kubernetes.io/name: {{ include "demo-app.name" . }} }
{{- end }}
